#include <stdio.h>

/**
 * main - Entry point
 *
 * This function prints a message to the console using the puts function.
 *
 * Return: always 0 (success)
 */

int main(void)
{
	printf("\"with proper grammar, but the outcome is a piece of art,");
	return (0);
}
